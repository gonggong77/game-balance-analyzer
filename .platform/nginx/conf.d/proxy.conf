client_max_body_size 100m;
proxy_read_timeout 600s;
proxy_send_timeout 600s;

# 큰 응답(멀티 에이전트 보고서)이 잘리지 않도록 버퍼 확장
proxy_buffer_size          128k;
proxy_buffers              8 256k;
proxy_busy_buffers_size    512k;
proxy_max_temp_file_size   0;
